export enum UserPasswordLengthRange {
  Min = 6,
  Max = 12,
}

export enum UserNameLengthRange {
  Min = 1,
  Max = 15,
}

export enum CaloriesLossRange {
  Min = 1000,
  Max = 5000,
}

export enum BurnsCaloriesPerDayRange {
  Min = 1000,
  Max = 5000,
}

export enum CoachMeritsLengthRange {
  Min = 10,
  Max = 140,
}

export const MAX_TRAINING_TYPE_LENGTH = 3;

export enum TrainingNameLengthRange {
  Min = 1,
  Max = 15,
}

export const MIN_TRAINING_PRICE = 0;

export enum TrainingDescriptionLengthRange {
  Min = 10,
  Max = 140,
}

export enum ReviewLengthRange {
  Min = 100,
  Max = 1024,
}

export enum NotifyLengthRange {
  Min = 10,
  Max = 140,
}

export enum ReviewValueRange {
  Min = 1,
  Max = 5,
}
